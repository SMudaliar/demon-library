import { Container} from "reactstrap";

const Header=()=>{
return(
    <div>
        <Container className="header1"style={{ marginTop: '10px' }}>
            <h1 className="text-center">Library Management System</h1>
        </Container>
    </div>
);
}
export default Header;